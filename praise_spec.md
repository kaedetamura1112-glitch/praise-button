# 褒めボタン 仕様書

最終更新：2026-06-03

---

## 1. システムの概要

日常・学習のモチベーション向上を目的としたシングルページのWebアプリケーション。ボタンをクリックするたびにランダムな日本語の褒め言葉が表示され、視覚的なフィードバック（ボタン色の変化・星のパーティクルアニメーション・フォント変化）によって使用者を肯定・激励する。

- **動作環境**：ブラウザで `index.html` を直接開くだけで起動。サーバー・ビルドツール・依存パッケージは不要。
- **公開方式**：GitHub Pages での静的サイト公開を前提とする（`index.html` がエントリポイント）
- **対象ユーザー**：作成者本人（日常の気分転換・学習前後のルーティンとして利用）

---

## 2. 機能一覧

| # | 機能名 | 概要 |
|---|--------|------|
| F-01 | 褒め言葉表示 | ボタンクリックで25種類からランダムに1件表示。直前と同じ内容は連続しない |
| F-02 | ボタン色サイクル | クリックごとに3色を順番に切り替える |
| F-03 | 星パーティクル | クリック時にボタン中心から星（★）が放射状に飛び出してフェードアウト |
| F-04 | フォントランダム | 褒め言葉を4種類のフォントからランダムに表示 |
| F-05 | 自動改行 | 13文字以上の褒め言葉を意味の切れ目で最大2行に分割 |
| F-06 | フェードインアニメーション | 褒め言葉が上から滑らかに現れる |

### F-05 自動改行ロジック詳細

`praise.js` の `insertLineBreak()` 関数で処理。

1. 12文字以下 → そのまま表示（改行なし）
2. 13文字以上 → 以下の優先順位で改行位置を探索（探索範囲：文字インデックス4〜12）
   - **第1優先**：句読点・感嘆符（`、` `。` `！` `？`）の直後
   - **第2優先**：助詞・接続助詞（`て` `で` `は` `が` `を` `に` `も` `と` `し`）の直後（探索開始：インデックス5）
   - **フォールバック**：12文字目で強制改行
3. 最大2行。2行目が12文字を超える場合は現状そのまま表示（PRAISES配列の内容で制御）

---

## 3. UI/UX定義

### 3-1. カラーパレット

| 用途 | カラーコード | 説明 |
|------|-------------|------|
| ボタン色①（初期色） | `#F45B69` | 赤系。影：`rgba(244, 91, 105, 0.45)` |
| ボタン色② | `#59C3C3` | ティール系。影：`rgba(89, 195, 195, 0.45)` |
| ボタン色③ | `#4062BB` | 青系。影：`rgba(64, 98, 187, 0.45)` |
| 背景 | `#faf7f4` | 温かみのあるオフホワイト |
| タイトル（h1） | `#aaa` | 控えめなグレー |
| ボタンテキスト | `#fff` | 白 |

- ボタン色とボタン影・褒め言葉テキスト色は常に同じ色で連動する
- 星パーティクルの色も現在のボタン色に連動する

### 3-2. フォント

すべて Google Fonts 経由で配信する Webフォントのみを使用。ローカルインストール不要。

| フォント名 | CSS family | weight | 用途 |
|-----------|-----------|--------|------|
| 源ノ角ゴシック JP Heavy | `'Noto Sans JP'` | 900 | ボタン本体・h1（固定）／褒め言葉（ランダム） |
| 源ノ角ゴシック JP Regular | `'Noto Sans JP'` | 400 | 褒め言葉（ランダム） |
| ポッタ | `'Potta One'` | 400 | 褒め言葉（ランダム） |
| 解星 春の海 Bold | `'Kaisei HarunoUmi'` | 700 | 褒め言葉（ランダム） |

- ボタン本体・h1 は `Noto Sans JP` weight:900 固定
- 褒め言葉（`#message`）のみクリックごとに上記4種からランダム切替（フォントファミリーは3種・ウェイトで4バリエーション）
- 平成丸ゴシック WB（有償・インストール必須）は廃止済み

### 3-3. アニメーション

#### ボタン
| 状態 | 変化内容 |
|------|---------|
| ホバー | `filter: brightness(1.06)` で明るさ微増 |
| 押下中（:active） | `transform: scale(0.92)` で縮小（押した感触） |
| クリック確定後 | 背景色・影を0.5秒 `ease` で次の色にトランジション |

#### 褒め言葉
- 非表示状態：`opacity: 0` / `transform: translateY(-12px)`（上方に12pxオフセット）
- 表示状態（`.show`クラス付与時）：`opacity: 1` / `translateY(0)` に0.35秒 `ease` でトランジション
- 切り替え時は `.show` を除去 → `requestAnimationFrame` × 2回後にテキスト更新 → `.show` 付与（ブラウザの再描画タイミングに合わせるための2段構え）

#### 星パーティクル（`spawnStars()`）
| パラメータ | 値 |
|------------|-----|
| 個数 | 10〜20個（ランダム） |
| サイズ | 30〜70px（ランダム） |
| 飛距離 | 150〜350px（ランダム） |
| 方向 | 0〜360°（ランダム） |
| 所要時間 | 800〜1000ms（ランダム） |
| イージング | `ease-out` |
| 終了処理 | アニメーション完了後に要素を自動削除（`onfinish: star.remove()`） |
| 実装方式 | Web Animations API（`element.animate()`） |

---

## 4. 使用技術

| 分類 | 技術・ツール |
|------|------------|
| マークアップ | HTML5 |
| スタイル | CSS3（Flexbox、CSS Transitions） |
| スクリプト | Vanilla JavaScript（ES6+） |
| アニメーション | Web Animations API |
| フォント配信 | Google Fonts（Webフォントのみ） |
| ビルドツール | なし（ファイルをブラウザで直接開いて動作） |
| ホスティング | GitHub Pages |
| 外部ライブラリ | なし |

---

## 5. 画面構成

```
┌─────────────────────────────────┐
│                                 │
│   ┌─────────────────────────┐   │
│   │   褒め言葉（#message）   │   │← 幅360px固定・中央揃え
│   │     最大2行表示          │   │  フォント・色はクリックごとに変化
│   └─────────────────────────┘   │
│                                 │
│         ┌──────────┐            │
│         │  押してね  │            │← 直径200pxの円形ボタン
│         └──────────┘            │  クリックで全要素が動作
│                                 │
│           褒めボタン             │← h1タイトル（控えめなグレー）
│                                 │
└─────────────────────────────────┘
背景色：#faf7f4
```

- 全体レイアウト：`display: flex; flex-direction: column; align-items: center; justify-content: center` でビューポート中央に配置
- 褒め言葉・ボタン・タイトルはすべて同一の水平中央軸上に揃う
- 星パーティクルは `position: fixed; z-index: 9999` でボタン上に重なるように描画

---

## 6. ファイル構成

```
praise/
├── index.html      # 画面構造・スタイル定義・フォント読み込み（GitHub Pages エントリポイント）
├── praise.js       # 定数・関数・イベント処理
└── praise_spec.md  # 本仕様書
```

### index.html の役割
- Google Fonts の読み込み（`<link>`）
- `body` / `h1` / `#praise-btn` / `#message` のスタイル定義
- HTML要素の配置（`#message` → `#praise-btn` → `h1` の順）
- `praise.js` の読み込み（`</body>` 直前）

### praise.js の役割

| 定数・変数 | 内容 |
|-----------|------|
| `COLORS` | ボタン色3色の配列 |
| `SHADOWS` | 各色に対応するbox-shadow色の配列 |
| `FONTS` | 褒め言葉用フォント4種の配列（family・weight） |
| `PRAISES` | 褒め言葉25件の配列 |
| `colorIndex` | 現在のボタン色インデックス（0〜2の循環） |
| `lastPraiseIndex` | 直前に表示した褒め言葉のインデックス（連続回避用） |

| 関数 | 役割 |
|------|------|
| `insertLineBreak(text)` | 13文字以上の文字列を意味の切れ目で改行 |
| `spawnStars(btn, color)` | 星パーティクルを生成・アニメーション・自動削除 |
| `btn.addEventListener('click', ...)` | クリック時の全処理（色変更・パーティクル・褒め言葉表示） |

---

## 7. 褒め言葉レパートリー（25件）

| # | テキスト |
|---|---------|
| 1 | 天才かもしれない… |
| 2 | 未来の自分が喜んでいる |
| 3 | 完璧じゃなくていい |
| 4 | よくできました！ |
| 5 | 今日も最高♪ |
| 6 | 無理しすぎなくていいよ |
| 7 | すごい！その調子！ |
| 8 | あなたは最高です！ |
| 9 | 頑張ってる、えらい！ |
| 10 | 今日もよく頑張った！ |
| 11 | 素晴らしい！ |
| 12 | あなたなら絶対できる！ |
| 13 | 継続は力なり、すごいよ！ |
| 14 | 一歩一歩、着実に進んでる！ |
| 15 | その努力、必ず実を結ぶ！ |
| 16 | 今この瞬間も成長してる！ |
| 17 | 今日のあなた、昨日より強い！ |
| 18 | 諦めないあなたが最高！ |
| 19 | 小さな一歩も、立派な前進！ |
| 20 | 休んでも大丈夫、また始めればいい！ |
| 21 | 毎日の積み重ねが未来を作る！ |
| 22 | やる気がなくても動いた、それだけで偉い！ |
| 23 | 自分を信じて進もう！ |
| 24 | あなたの努力は無駄じゃない！ |
| 25 | 完璧じゃなくていい、続けることが大事！ |

---

## 8. 今後の改善案

### 機能追加
- **褒め言葉のカテゴリ分け**：学習用・仕事用・疲れた日用などのモード切替
- **表示回数のカウント・記録**：localStorage でクリック数を永続保存し、マイルストーン達成時に特別メッセージを表示
- **音響エフェクト**：Web Audio API でポップな効果音を追加

### UI改善
- **スマートフォン対応**：タップ領域・フォントサイズをモバイル向けに調整
- **ダークモード対応**：OSの配色設定に応じて背景色・テキスト色を切り替え

### コード品質
- `PRAISES` 配列6番目と7番目の要素間にカンマ漏れがある（構文エラーの可能性）。確認・修正推奨。
